//Arquivo para definir os types gerais da aplicação
interface Estudante {
    name: string;
    idade: number;
}
interface Estudante {
    serie: string;
}

//usando o construtor, soma-se as duas interfaces
const estudante: Estudante = {
     idade;
     name;
     serie;
}